httpRoute:
  enabled: true
  host: "smoke-test.kind.local"
  parent:
    name: private-http
    namespace: gateways
    sectionName: http
  backend:
    name: smoke-test
    port: 80
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "smoke-test.kind.local"
    external-dns.alpha.kubernetes.io/target: "192.168.4.124"
